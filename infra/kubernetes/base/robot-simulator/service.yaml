apiVersion: v1
kind: Service

metadata:
  name: robot-simulator
  labels:
    app: robot-simulator

spec:
  type: NodePort

  selector:
    app: robot-simulator

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080